{
  "name": "flamingo-wallet",
  "version": "0.0.1",
  "description": "Flamingo Wallet UI",
  "main": "src/send_receive_button.js",
  "scripts": {
    "start": "budo web/boot.js:bundle.js --dir ./ --live --open",
    "build": "browserify web/boot.js > bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "button"
  ],
  "Dependencies": {
    "budo": "^11.8.4",
    "browserify": "^17.0.1"
  },
  "devDependencies": {
    "standardx": "^7.0.0"
  },
  "standardx": {
    "ignore": [
      "bundle.js"
    ],
    "rules": {
      "camelcase": 0
    }
  }
}
